import React from 'react'

const VerticalPath = (cells,color) => {
  return (
    <div className='flex-row items-center w-[20%] h-full'>
        <div className='flex-col w-full h-full'>

        </div>

    </div>
  )
}

export default React.memo(VerticalPath);